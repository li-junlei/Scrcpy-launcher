# Rust 编译产物
/target/
**/*.rs.bk
*.pdb

# Cargo
Cargo.lock

# Tauri 构建产物
/src-tauri/target/
gen/

# 前端构建产物 (如果 dist 目录是编译生成的)
# dist/
# 注意：如果 dist 目录包含源代码（如本项目的 HTML/JS/CSS），则不应忽略

# Tauri 打包产物
*.msi
*.exe
*.app
*.dmg
*.deb
*.rpm
*.AppImage
/bundle/

# Node.js 依赖（如果有前端构建流程）
node_modules/

# IDE 和编辑器
.vscode/
.idea/
*.swp
*.swo
*~
.cursor/

# 操作系统文件
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# 用户配置文件（不应提交到版本控制）
config.json

# 日志文件
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 环境变量文件
.env
.env.local
.env.*.local

# 临时文件
*.tmp
*.temp
.tmp/
.temp/

# 测试覆盖率
/coverage/

# 资源文件中的二进制文件
# 本项目选择将这些文件提交到仓库以确保开箱即用
# 如果仓库过大，可以考虑使用 Git LFS：
# git lfs track "resources/bin/*"
# git lfs track "dist/app_icons/*"

# 应用图标资源（如果有大量的应用图标缓存）
# dist/app_icons/*.png

# 测试版本
/test_version
CHANGELOG.md
src/CLAUDE.md
dist/CLAUDE.md
CLAUDE.md
CLAUDE.md
/reference